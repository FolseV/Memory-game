export type CardType = {
  type: string;
  image: string;
};

export interface sortedLeaderBoardType {
  difficulty: string;
  email: string;
  firstName: string;
  lastName: string;
  moves: number;
  suite: string;
  time: number;
}
